import { User } from "../models/index.models.js"

export const userController = {

    getOrCreate: async (req, res) => {

        try {
            
        } catch (error) {
            
        }
    }
}